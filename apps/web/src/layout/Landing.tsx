import HeroSection from "@/shared/HeroSection";
import Navbar from "@/shared/Navbar";

const Landing = () => {
    return(
        <section>
            <Navbar/>
            <HeroSection/>
        </section>
        
    );
}

export default Landing;